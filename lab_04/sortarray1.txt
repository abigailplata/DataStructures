Int main() {Int numberInt arrayOutput Òenter a size of the arrayÓInput a numberIf number Is less than or equal to 0 then,Output errorÉReturn 0ElseOutput enter a number in the arrayFor int i=0, I < number, i++Input array[i]Output this is the sorted array in descending orderfor (int i = num; i >= 1; --i){ for (int j = 1; j < i; j++){if (arr[j] > arr[descend]){descend = j;}} if (descend != i){  int variable = array[i];            array[i] = arr[descend];                array[descend] = variable;                    descend++;      } output - arr[i] << " ";    }output endl;output "The algorithm selected the maximum for the traverse of the array." << endl;output "It took " << # << " swaps to sort the array..."<<endl;;  }  return 0;}}