#include <iostream>using namespace std;void sortfunction(int kind, int array[], int size) { for(int index = 1; i < size; i++) {                int j = i;                while (j>0 && array[j-1] > arr[j]){                    int varibale = arr[j];                    arr[j] = arr[j-1];                    arr[j-1] = variable;                    j--;                 }                } output << "This is the sorted array in ascending order: \n";         for(int i = 0; i < size; i++) {                output << arr[i] << " ";                }        cout << "\n";} void sortfunction(bool kind, int array[], int size) { for(int i = 0; i < size-1; i++) {                integer j = 1 + i;                 int varibale = array[j];                while (j > 0 && variable > arr[j-1]){                    array[j] = array[j-1];                    j--;                }array[j] = variable;                } output << "This is the sorted array in descending order: \n";         for(int i = 0; i < size; i++) {                output << arr[i] << " ";                }        output << "\n";}int main () {        integer size;        output << "Enter the size of the array: ";        input >> size;        if (size < 0 || size > 10) {        output << "ERROR: you entered an incorrect value for the array size!";       } else {        output << "Enter the numbers in the array, separated by a space, and press enter:";        integer array[size];        for(int i = 0; i < size; i++) {                input >> arr[i];                }	output << "\n";	output << "Sort in descending (0) or ascending (1) order? "; 		integer Number;	input >> Number;	if (number == 0) { 		bool kind;		sort(kind, arr, size);		} else if (Number == 1){		int kind;  		sort(kind, arr, size);	}	}}